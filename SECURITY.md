# 安全策略

## 支持范围

BalanceBar 会为默认分支和最新发布版本处理安全问题。旧版本通常只在问题仍影响最新版本时一并评估，不承诺单独回补。

| 版本 | 安全更新 |
| --- | --- |
| `main` | 支持 |
| 最新发布版本 | 支持 |
| 更早版本 | 通常不支持 |

## 私下报告漏洞

请不要为尚未修复的安全漏洞创建公开 Issue、Discussion 或 Pull Request。

请通过 [@huanmeng06 的 GitHub 个人资料](https://github.com/huanmeng06)中提供的私密联系方式联系维护者。如果当时没有可用的私密联系方式，可以创建一个**不包含漏洞细节**的公开 Issue，仅请求维护者提供安全联系渠道。

报告中请尽量包含：

- 受影响版本、提交或功能；
- 可复现的最小步骤或概念验证；
- 实际影响、攻击前提和受影响数据；
- 已确认不受影响的边界；
- 建议的验证方式；
- 你希望在公告中使用的署名，或说明希望匿名。

发送日志、截图或样本前，请删除 API Key、Token、Cookie、完整 endpoint、真实账号信息、CC Switch 数据库及其他凭据。

## 响应流程

维护者会尽量在 7 天内确认收到报告，并在完成初步复现后告知风险判断和后续安排。修复时间取决于问题复杂度、影响范围和发布风险。

在修复发布前，请给维护者合理时间完成验证与发布，不要公开利用细节。修复完成后，维护者会在适当情况下发布安全公告并感谢报告者。

## 范围说明

BalanceBar 会读取本机 CC Switch、Codex 和 Claude Code 的配置或凭据，并访问所配置供应商的接口。以下内容本身不一定构成 BalanceBar 漏洞：

- 第三方供应商、CC Switch、Codex 或 Claude Code 自身的漏洞；
- 用户主动安装或配置的不可信脚本、供应商或 endpoint；
- 仅在已取得本机用户权限并能直接读取同一文件时成立、且没有扩大权限或数据暴露的问题。

如果不确定问题是否在范围内，请仍然按私密方式报告，维护者会协助判断归属。
